# Ask Siraj. Set these on the HOSTING PROVIDER, not as GitHub Actions secrets:
# an Actions secret never reaches a deployed function or the browser.
OPENAI_API_KEY=sk-...
# the exact model id from your OpenAI project
OPENAI_MODEL=
